    steps:
    - name: Baixar o código
      uses: actions/checkout@v4 # Atualize para v5 se já estiver disponível

    - name: Configurar Java 17
      uses: actions/setup-java@v5 # Correção apontada no aviso da imagem
      with:
        java-version: '17'
        distribution: 'temurin'
